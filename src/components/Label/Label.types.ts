/**
 * **LabelProps**
 * Defines the properties for the `Label` component.
 */
export type LabelProps = {
    text: string;       // The label text
    disabled?: boolean; // Whether the label is disabled
    htmlFor?: string;   // The "for" attribute (for input association)
  };
  